import { Container } from "@mui/system";
import { Typography } from "@mui/material";

const AccountSettings = () => {
  return (
    <Container>
      <Typography variant="h5">Account settings component</Typography>
      <br />
    </Container>
  )
}

export default AccountSettings;